<!-- import using code fragment: $this->fetch('termsofuse.phtml'); -->
<h3>Terms of Use</h3>
<ul>
    <li>one(1) request per second per key or IP-address</li>
    <li>10000 requests per day per key or IP-address</li>
    <li>lists of addresses are restricted to 5 rows per response</li>
    <li>keys expire after 9 months(after last usage)</li>
</ul>